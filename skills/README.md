# Skills

Skills are reusable instructions that teach an AI agent how to perform a specific Vatras operation.

A skill defines **what the operation is, when to use it, what context it needs, how it should be performed, and what it produces**.

## Structure

Each skill lives in its own directory:

```text
skills/
└── <skill-name>/
    └── SKILL.md
```
